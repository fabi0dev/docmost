# =============================================================================
# Database (PostgreSQL)
# =============================================================================
# Local (Docker): use a mesma URL em ambos
#   DATABASE_URL="postgresql://user:password@localhost:5432/docmost?schema=public"
#   DIRECT_URL="postgresql://user:password@localhost:5432/docmost?schema=public"
#
# Supabase:
#   DATABASE_URL = Pooling > Transaction (URI, porta 6543) + ?pgbouncer=true no final (evita erro 42P05)
#   DIRECT_URL  = Connection string direta (URI, porta 5432) — migrations
#   Em Project Settings > Database copie as URIs e substitua [YOUR-PASSWORD]
DATABASE_URL="postgresql://user:password@localhost:5432/docmost?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/docmost?schema=public"

# =============================================================================
# NextAuth
# =============================================================================
# Local: http://localhost:3000
# Vercel: use a URL do deploy (ex: https://seu-projeto.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="gere-uma-chave-secreta-openssl-rand-base64-32"
# Na Vercel: defina AUTH_TRUST_HOST=true para evitar erro de host nas rotas de auth

# =============================================================================
# Supabase (opcional: Realtime, Storage, Auth futura)
# =============================================================================
# No dashboard: Project Settings > API > Project URL e anon/public key
NEXT_PUBLIC_SUPABASE_URL="https://seu-projeto.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sua-anon-key"

# =============================================================================
# App
# =============================================================================
# Na Vercel: NODE_ENV é definido automaticamente (production)
NODE_ENV="development"

# =============================================================================
# Deploy (Vercel)
# =============================================================================
# No dashboard da Vercel: Settings > Environment Variables
# Obrigatório: NEXTAUTH_URL (URL do deploy), NEXTAUTH_SECRET, DATABASE_URL, DIRECT_URL
# Recomendado: AUTH_TRUST_HOST=true (evita erro de host no NextAuth na Vercel)

# =============================================================================
# Groq (chat IA)
# =============================================================================
GROQ_API_KEY=""
GROQ_MODEL="openai/gpt-oss-120b"
GROQ_MODEL_CLASSIFY="llama-3.1-8b-instant"
